<VirtualHost *:80>

    ServerName localhost

    ProxyPreserveHost On

    ProxyPass        / http://service_bridge:8761/
    ProxyPassReverse / http://service_bridge:8761/

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined

</VirtualHost>
